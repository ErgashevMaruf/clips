import { EventPreventDirective } from './event-prevent.directive';

describe('EventPreventDirective', () => {
  it('should create an instance', () => {
    const directive = new EventPreventDirective();
    expect(directive).toBeTruthy();
  });
});
